extends /main

block content
  section.pure-section.pure-padding--5rem
    .pure-container.pure-container--vertical
      h1.white-text Installation
      ul.pure-tabs.getting-started
        li.active(data-target="#getting-started-cdn")
          h2.title CDN
          p Use a CDN to speed up the loading time of your application.
        li(data-target="#getting-started-npm")
          h2.title NPM
          p Host the JavaScript on your own server.
      .getting-started-spacer
      .getting-started-items
        .item.white-text.pure-padding--5rem#getting-started-cdn
          h2.title.white-text #[a.copy.white-text(data-target='#copy-cdn' data-tooltip='Copy code') #[i.fas.fa-clipboard]] CDN
          pre#copy-cdn= '<script src="https://cdn.horsepowerjs.com/latest/hp.min.js"></script>'
        .item.white-text.hidden.pure-padding--5rem#getting-started-npm
          h2.title.white-text #[a.copy.white-text(data-target='#copy-npm' data-tooltip='Copy code') #[i.fas.fa-clipboard]] NPM
          pre#copy-npm= 'npm install horsepower --save'
  section.pure-section.white
    .pure-container.pure-container--vertical.pure-padding-top--5rem.pure-padding-bottom--5rem
      h1.is-size-1 Usage
      div: p.pure-block Basic HTML setup requires that you include the library first, then include the javascript for the page afterwards.
      pre: code.language-html
        = '<!doctype html>'
        = '<html>'
        = '<head>'
        = '<script src="https://cdn.horsepowerjs.com/latest/hp.min.js"></script>'
        = '<script src="/app.js"></script>'
        = '</head>'
        = '<body>'
        = '<p></p>'
        = '</body>'
        = '</html>'
      div: p.pure-block Here in the javascript file #[code app.js] we then create a class, and use it to observe the page for changes. Once the #[code p] tag has been observed horsepower will begin to send #[code messages] to it if the messages are implmented.
      pre: code.language-javascript
        = 'class paragraph extends hp.element {'
        = '  created() {'
        = '    this.text = \'Hello World\''
        = '  }'
        = '}'
        = 'hp.observe(\'p\', paragraph)'